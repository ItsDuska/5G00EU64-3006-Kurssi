# Game of Life

### Arguments:

```bash
./life 32 64
```

The two arguments are the x and y values of the Grid.

### Build

Works on Arch linux, but doesn't work on Widows.

```bash
cmake -S . -B build
cd build
cmake --build .
```
